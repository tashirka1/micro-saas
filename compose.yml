volumes:
  caddy_data:
    driver: local
  caddy_config:
    driver: local

services:
  web:
    restart: always
    build:
      dockerfile: ./Dockerfile
      context: ./web
    env_file: web/.env

  caddy:
    restart: always
    image: caddy:2.11
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./devops/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
